apiVersion: v1
kind: Service
metadata:
  name: {{ include "direct-bedrock-proxy.name" . }}
  labels:
    app.kubernetes.io/name: {{ include "direct-bedrock-proxy.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: {{ include "direct-bedrock-proxy.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
