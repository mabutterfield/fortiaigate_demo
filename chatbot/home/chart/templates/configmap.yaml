apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "demo-home.name" . }}
  labels:
    app.kubernetes.io/name: {{ include "demo-home.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
data:
  index.html: |
{{ .Values.home.html | indent 4 }}
