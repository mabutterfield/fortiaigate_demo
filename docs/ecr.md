# ECR Documentation Has Moved

[Container Repository Management](container-repository-management.md) is the
single source of truth for Docker image inputs, tags, ECR infrastructure,
local registries, publishing, verification, cleanup, rollback, and current
quickstart integration.

This short pointer preserves the established `docs/ecr.md` path without
maintaining a second copy of the workflow.
